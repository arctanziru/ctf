name: Test Action for CTF
on:
  push:
    branches:
      - main

jobs:
  request-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Make HTTP POST Request
        run: |
          curl -X POST https://actions.ctf.cert.unlp.edu.ar \
          -H "Content-Type: application/json" \
          -d '{"key": "value"}'
